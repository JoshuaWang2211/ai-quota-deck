{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the dashboard and widget windows",
  "windows": ["main", "widget"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
